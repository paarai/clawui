# 2026-03-10

## GUI 自动化项目
- 用户需求：Linux 平台 GUI 自动化工具，支持 AI 驱动，集成 OpenClaw
- 方案：基于 Anthropic Computer Use 改造 + AT-SPI 混合感知
- 代码在 `projects/gui-automation/`
- 进度 95%，已完成：
  - AT-SPI 集成（枚举应用、控件树、操作）
  - 截图模块（Wayland D-Bus portal + grim + X11 scrot）
  - 键鼠操作（ydotool Wayland + xdotool X11）
  - 多模型后端（Claude + GPT-4o + Gemini）
  - Agent 决策循环 + 错误恢复
  - OpenClaw SKILL.md
- 剩余：端到端测试、GNOME Wayland 截图权限配置

## 系统环境
- Wayland (GNOME)，有 XWayland 兼容层
- ydotool、xdotool、grim、scrot 均已安装
- AT-SPI 能看到 15 个应用

## 会话
- 之前的会话重置了，用户通过转发聊天记录恢复上下文
- 用户通过飞书沟通
## Bootstrap 完成
- 我的名字：龙虾 🦞
- 用户：KK，风格简洁直接，时区 Asia/Shanghai，通过飞书沟通
- IDENTITY.md、USER.md 已更新
- BOOTSTRAP.md 还没删（等确认后删）

## 修复记录
- ydotool 0.1.8 崩溃：`--absolute` 参数不支持，改为优先 xdotool（通过 XWayland 在 Wayland 下工作正常）
- `/dev/uinput` 权限：chmod 666 + udev 规则持久化
- WeappLocalData 文件锁冲突：清理残留 lock 文件
- Apport 崩溃报告弹窗：systemctl disable apport + /etc/default/apport enabled=0
- gnome-screenshot 在 Wayland 下通过 XWayland 正常工作

## GUI Skill 模式
- 改为 OpenClaw agent 直接驱动模式（我自己看屏幕+决策+操作），不需要外部 AI API
- AnyRouter 不支持外部脚本调用（只限 OpenClaw 内部）
- 保留了多后端接口（Claude/GPT-4o/Gemini/Ollama）供未来自主运行模式使用
- Ollama 已安装但未拉模型（KK 说不需要本地跑）

## 微信开发者工具
- snap 安装：`snap run wechat-devtools`
- 启动时有"加载扩展程序时出错"弹窗（无害，点确定关掉）
- 等待 KK 扫码登录中
- 二维码截图已发送给 KK
