### ClawUI Continuous Improvement (00:10 - cron)

**Improvements implemented:**

1. **New tool: `wait_for_element`** — polls AT-SPI with exponential backoff until a UI element appears or timeout. Critical for reliable automation (previously only `wait_for_window` existed; many automations need to wait for specific buttons/fields to appear after transitions).

2. **Comprehensive test suite: `tests/test_core.py`** — 21 tests covering:
   - Tool registration (count, duplicates, schema validation)
   - All backend imports (AT-SPI, X11, CDP, Marionette, Perception, OCR, Recorder)
   - AT-SPI operations, X11 window listing, screenshots
   - Tool execution (ui_tree, list_windows, find_element, wait_for_element timeout)
   - Perception routing
   - All 11 non-GUI tests pass; 10 GUI tests skip gracefully in headless env

**GitHub Issues:** 0 open issues on longgo1001/clawui
**Commit:** `20bcc35` pushed to origin/main
**Tool count:** 48 tools, 0 duplicates

### ClawUI Continuous Improvement (01:15 - cron)

**Fixed 4 bugs in agent.py:**
1. **Variable shadowing**: `find_element` handler overwrote outer `name` parameter with `input_data.get("name")` → renamed to `el_name`
2. **Inconsistent imports**: 12 occurrences of `from src.` (absolute) changed to `from .` (relative) — prevents ImportError when loaded as package
3. **`consecutive_errors` scoping**: was reset to 0 inside loop body every iteration, making the "3 consecutive errors" check impossible to trigger → moved outside loop
4. **`result` variable shadowing**: `result` used for both backend response and tool execution result in `run_agent()` → renamed to `response`/`tool_result`/`last_response`

**Tests:** 11/21 passed, 0 failed, 10 skipped (headless env)
**GitHub Issues:** 0 open
**Commit:** `8fbc80e` pushed to origin/main
