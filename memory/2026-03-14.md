
### ClawUI Continuous Improvement (00:58 - cron)

**Implemented: Smart CDP Profile Selection for Snap Chromium**
- Root cause: snap chromium can't write to `~/.local/share/clawui/chromium_profile` due to confinement → `SingletonLock: Permission denied` errors on every launch attempt
- Fix: `_profile_dirs_for_launcher()` now selects profile dirs based on launcher type:
  - Snap launchers → `~/snap/chromium/common/clawui-profile-{port}`
  - Non-snap launchers → standard `~/.local/share/clawui/chromium_profile`
- Also: auto-cleans stale SingletonLock files, uses deterministic temp paths instead of `mkdtemp`
- All 4 e2e tests pass (AT-SPI ✅, X11 ✅, CDP ✅, Marionette ✅)
- CDP now connects cleanly without permission error spam
- **GitHub Issues**: 0 open
- **Commit**: `3b28f9a` pushed to main

### ClawUI Continuous Improvement (02:03 - cron)

**Fix: Proper snap Chromium wrapper detection in `_is_snap_launcher`**
- Previous code only matched `'snap' in launcher` (the literal command list)
- On Ubuntu, `chromium-browser` is `/usr/bin/chromium-browser` — a shell script that delegates to `/snap/bin/chromium`
- New `_is_snap_launcher()` reads the binary/script content to detect `/snap/` references
- This ensures correct profile dir selection (~/snap/chromium/common/) and avoids `SingletonLock: Permission denied` errors
- All 22/22 core tests pass
- **GitHub Issues**: 0 open
- **Commit**: `aa72b3b` (ClawUI) + `227ca07` (workspace), pushed to main

### ClawUI Continuous Improvement (03:11 - cron)

**Repo Organization + Packaging**
- Moved 14 test files into `tests/` and 13 demo scripts into `demos/` — repo root now clean
- Added `pyproject.toml` (pip installable as `clawui` CLI) + MIT LICENSE
- All 22/22 core tests pass
- **GitHub Issues**: 0 open (gh CLI not authenticated, checked manually)
- **Commit**: `f58503d` pushed to main
- No significant milestone to report to KK

### ClawUI Continuous Improvement (04:17 - cron)
- **Implemented**: CLI entry point (`src/cli.py`) with proper subcommands
  - `clawui run "task"` - run agent
  - `clawui apps` - list running apps
  - `clawui tree` - show UI tree
  - `clawui screenshot` - take screenshot
  - `clawui test` - run test suite
  - `clawui version` - show version
- **Merged**: pyproject.toml conflict with remote (remote had already added basic pyproject.toml; merged with our improvements: dev extras, X11 classifier, cli entry point)
- **Tests**: 22/22 pass, CLI verified working (`clawui version`, `clawui apps`)
- **GitHub Issues**: Cannot check (gh CLI not authenticated)
- **Commit**: `eed9286` pushed to main
- **TODO**: All items complete. Project is mature; future work: user-reported issues, real-world testing.

### ClawUI Continuous Improvement (05:21 - cron)
- **Implemented**: `wait_for_element` and `wait_for_text` tools in agent.py
  - `wait_for_element`: polls AT-SPI for element by role/name with timeout+interval
  - `wait_for_text`: polls OCR screenshots for text appearance with timeout+interval
  - These were marked done in TODO but were actually missing from agent.py
- **GitHub Issues**: 0 open issues on longgo1001/clawui
- **Commit**: `c67f305` pushed to main

### ClawUI Continuous Improvement (06:27 - cron)
- **Implemented**: `click_element` and `get_element_text` high-level tools
  - `click_element`: find element by role/name → click center in one step (left/right/double), with retry
  - `get_element_text`: read element name/value/states as JSON
  - Eliminates the most common 2-call pattern (find_element → click) into 1 call
  - Total tools: 54
- **Tests**: All 4 backends pass (AT-SPI ✅, X11 ✅, CDP ✅, Marionette ✅)
- **GitHub Issues**: 0 open issues
- **Commit**: `a3c02f2` pushed to main
### ClawUI Continuous Improvement (07:31 - cron)
- **Implemented**: Persistent WebSocket connection for CDP backend
  - Previously: every CDP command opened a new WS connection (~50-100ms overhead)
  - Now: reuses persistent WS, auto-reconnects on failure, skips CDP events
  - Benchmark: 3.1ms/call (was ~50-100ms) = ~15-30x speedup
  - Critical for typing (dispatch_key sends 2 msgs per char) and rapid interactions
- **GitHub Issues**: 0 open issues on longgo1001/clawui
- **Commit**: `6a6d88c` pushed to main
- **TODO**: All items complete; project in maintenance/improvement mode

### ClawUI Continuous Improvement (08:35 - cron)
- **Implemented**: `cdp_get_elements` tool — the web equivalent of `ui_tree` for browser pages
  - Extracts all interactive elements (buttons, links, inputs, selects, [role] elements) from current page
  - Returns: tag, type, text/label, CSS selector, ARIA role, bounding box, current value
  - Formats as indexed list for easy reference by the agent
  - Enables structured web page understanding without guessing CSS selectors
  - Total tools: 55
- **Tests**: 22/22 pass (AT-SPI ✅, X11 ✅, CDP ✅, Marionette ✅)
- **GitHub Issues**: 0 open issues on longgo1001/clawui
- **Commit**: `8bcee86` pushed to main

### Heartbeat Check (09:10)
- 按 HEARTBEAT.md 检查：TODO.md 全部已完成，无可安全自动执行的新任务。
- Git 状态：`M ClawUI`, `M memory/2026-03-14.md`, `?? tools/launch_wechat_devtools.sh`。
- 微信开发者工具 Wine 兼容排障进展：
  - 已升级到 `wine-staging 11.4`（从 wine 9.0）。
  - 确认当前工具版本：微信开发者工具 `2.01.2510280`，NW.js `0.54.1`（内嵌 Chromium 91）。
  - 在 `--no-sandbox --disable-gpu --use-gl=swiftshader` 下仍出现启动后静默退出（无窗口）。
  - 结论：仅升级 Wine 仍不足以彻底解决该版本 NW.js 渲染兼容问题，后续需新前缀+运行库组合或切换 Linux 原生版。
