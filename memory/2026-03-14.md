# 2026-03-14 Daily Notes

## 微信开发者工具 Wine 启动成功 (08:30-10:12)
- **问题**: 微信开发者工具 (v2.01.2510280, NW.js 0.54.1/Chromium 91) 在 Wine 下无法启动
- **解决步骤**:
  1. Wine 9.0 → wine-staging 11.4 (WineHQ 官方源)
  2. Wine 11.4 默认用 Wayland 驱动 → 强制 X11: `wine reg add "HKCU\Software\Wine\Drivers" /v Graphics /d x11 /f`
  3. `wechatdevtools.exe` 直接启动只显示 NW.js 默认页 → 需传入 `code/package.nw` 应用路径
- **最终启动命令**:
  ```bash
  cd "$HOME/.wine/drive_c/Program Files (x86)/Tencent/微信web开发者工具/"
  wine wechatdevtools.exe "code/package.nw" --no-sandbox --disable-gpu --disable-gpu-compositing --in-process-gpu
  ```
- **启动脚本**: `tools/launch_wechat_devtools.sh`
- **状态**: 成功登录并进入主界面（小程序/小游戏项目页面）

## 飞书图片发送问题排查 (09:33-10:07)
- **问题**: Agent 回复中的图片无法发送到飞书
- **根因**: 
  1. Agent 用 `read` 工具查看图片只在 agent 端渲染，不会发到飞书
  2. `normalizePossibleLocalImagePath` 要求消息是纯路径（无空格/其他文本），agent 回复通常包含上下文文字
  3. `/tmp` 不在 `mediaLocalRoots` 允许列表中 → `LocalMediaAccessError: path-not-allowed`
- **解决**: 使用 `openclaw message send --channel feishu --account main --target <user_id> --media <workspace_path> -m "caption"` 直接发送
- **允许的媒体目录**: `~/.openclaw/{media,agents,workspace,sandboxes}` + OpenClaw 专用 tmp 目录
- **教训**: 向飞书发图片时，图片必须放在 workspace 目录下，然后用 `openclaw message send` CLI 发送

## ClawUI 持续改进 (cron 自动)
- 00:58: Snap Chromium profile 路径修复 (commit 3b28f9a)
- 02:03: snap launcher 检测改进 (commit aa72b3b)
- 03:11: 仓库整理 + pyproject.toml 打包 (commit f58503d)
- 04:17: CLI 入口 clawui run/apps/tree/screenshot/test/version (commit eed9286)
- 05:21: wait_for_element/wait_for_text 工具实现 (commit c67f305)
- 06:27: click_element/get_element_text 高层工具 (commit a3c02f2)
- 07:31: CDP 持久化 WebSocket 连接, 15-30x 加速 (commit 6a6d88c)

## 微信开发者工具 Wine 问题深度修复 (10:12-11:03)
- **模拟器黑屏 + 按钮无响应**:
  - 项目创建成功（飞机大战模板 minigame-1），但 IDE 模拟器黑屏、编译/预览按钮无反应
  - 根因1: Wine 默认模拟 Win7，微信开发者工具 1.06+ 不再支持 Win7
  - 根因2: GPU/Canvas 渲染在 Wine 下需要 swiftshader 后端
  - 修复: `winecfg /v win10` 切换到 Win10 + 清理 nwjs GPU/Shader/Code Cache
  - 修复后 UI 交互恢复正常（创建项目弹窗可正常操作）
- **小游戏项目已创建**:
  - 路径: `~/.wine/drive_c/users/hung/WeChatProjects/minigame-1/`
  - 模板: 飞机大战（hero/enemy/bullet/explosion/bgm）
  - 需继续: 编译 + 模拟器预览 + 真机测试
- **启动脚本已更新**: `~/launch_wechat_devtools.sh`（含 Win10 设置 + 缓存清理 + swiftshader）
- **ClawUI 自动化微信开发者工具的关键经验**:
  - Wine 应用不在 AT-SPI 树中，必须用 xdotool 坐标 + 截图驱动
  - 菜单栏点击需精确像素定位（OCR 在 Wine 窗口中不可靠）
  - 绿色按钮可通过 PIL/numpy 颜色匹配找到坐标

## ClawUI CI/Test Suite (cron 11:55)
- **Implemented**: Unit test suite (14 tests) + GitHub Actions CI + fixed e2e tests
  - `tests/test_unit.py`: 14 unit tests covering screenshot, CDP, actions, X11, agent tools, recorder, OCR, perception, CLI — all run without display server
  - `tests/test_e2e.py`: rewritten with proper pytest assertions and skip decorators (no more return-bool warnings)
  - `tests/conftest.py`: shared path setup
  - `.github/workflows/ci.yml`: runs unit tests on Python 3.10/3.11/3.12 + syntax lint
- **All tests pass**: 14 unit + 4 e2e = 18 tests green
- **Commit**: `3fdfcec` pushed to main
- **GitHub Issues**: gh CLI not authenticated; could not check (known issue from 2026-03-13)

## ClawUI: Annotated Screenshots Feature (cron 13:03)
- **Implemented**: `annotated_screenshot` + `click_by_index` tools (commit a117629)
  - `src/annotated_screenshot.py`: Takes screenshot, detects interactive elements via AT-SPI tree walk or CDP DOM query, overlays numbered red labels
  - `click_by_index`: Click any labeled element by number — no more coordinate guessing
  - Agent loop updated to handle `image_and_text` tool result type
  - Tested: 20 elements detected on current desktop (buttons, menus, toggles)
- **Test suite fixed**: Renamed `test()` helper → `run_test()` to avoid pytest fixture conflict; removed `return True` warnings
  - 23/23 tests pass, 0 errors, 0 warnings (down from 1 error + 25 warnings)
- **GitHub Issues**: gh CLI not authenticated; cannot check (known issue)
- **Impact**: This is the #1 reliability improvement for AI-driven GUI automation — numbered element labels eliminate coordinate guessing errors
- **Total tools**: 58 (up from 56)

## Heartbeat Checkpoint (13:30)
- TODO.md 检查完成：当前条目均已完成，无需额外安全执行任务。
- 微信基础库下载进展：`3.14.2.wxvpkg` 已通过直连下载成功（约 34MB），替换了此前 0-byte 损坏文件。
- NVIDIA API 测试：`https://integrate.api.nvidia.com/v1` + `z-ai/glm4.7` 连通，返回结果主要在 `reasoning_content` 字段。
- 工作区 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`, `s2.png`, `s3.png`, `s4.png`, `s5.png`, `s6.png`

## ClawUI Continuous Improvement (cron 14:12)
- **Reviewed**: TODO.md (all items complete), GitHub issues (0 open)
- **Found**: Remote already had annotated_screenshot from earlier cron (commit a117629), but our local was behind
- **Action**: Rebased local changes, added 3 unit tests for annotated_screenshot module (import, dedup, LabeledElement serialization)
- **Tests**: 17/17 unit tests pass
- **Commit**: `41bd129` pushed to main
- **Tool count**: 60 agent tools
- **Status**: Project is mature — all core features implemented, CI green, no user issues

## Heartbeat Checkpoint (13:58)
- TODO.md 检查完成：无新增可安全自动执行任务。
- 记忆维护检查：MEMORY.md 已包含今日关键结论（微信基础库 IPv4 下载可绕过、NVIDIA Integrate 返回 `reasoning_content` 兼容性要点），暂无额外需补充项。
- 工作区 git 状态（仍有未提交变更）：
  - Modified: `ClawUI`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`, `s2.png`, `s3.png`, `s4.png`, `s5.png`, `s6.png`
