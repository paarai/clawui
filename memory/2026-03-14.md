# 2026-03-14 Daily Notes

## 微信开发者工具 Wine 启动成功 (08:30-10:12)
- **问题**: 微信开发者工具 (v2.01.2510280, NW.js 0.54.1/Chromium 91) 在 Wine 下无法启动
- **解决步骤**:
  1. Wine 9.0 → wine-staging 11.4 (WineHQ 官方源)
  2. Wine 11.4 默认用 Wayland 驱动 → 强制 X11: `wine reg add "HKCU\Software\Wine\Drivers" /v Graphics /d x11 /f`
  3. `wechatdevtools.exe` 直接启动只显示 NW.js 默认页 → 需传入 `code/package.nw` 应用路径
- **最终启动命令**:
  ```bash
  cd "$HOME/.wine/drive_c/Program Files (x86)/Tencent/微信web开发者工具/"
  wine wechatdevtools.exe "code/package.nw" --no-sandbox --disable-gpu --disable-gpu-compositing --in-process-gpu
  ```
- **启动脚本**: `tools/launch_wechat_devtools.sh`
- **状态**: 成功登录并进入主界面（小程序/小游戏项目页面）

## 飞书图片发送问题排查 (09:33-10:07)
- **问题**: Agent 回复中的图片无法发送到飞书
- **根因**: 
  1. Agent 用 `read` 工具查看图片只在 agent 端渲染，不会发到飞书
  2. `normalizePossibleLocalImagePath` 要求消息是纯路径（无空格/其他文本），agent 回复通常包含上下文文字
  3. `/tmp` 不在 `mediaLocalRoots` 允许列表中 → `LocalMediaAccessError: path-not-allowed`
- **解决**: 使用 `openclaw message send --channel feishu --account main --target <user_id> --media <workspace_path> -m "caption"` 直接发送
- **允许的媒体目录**: `~/.openclaw/{media,agents,workspace,sandboxes}` + OpenClaw 专用 tmp 目录
- **教训**: 向飞书发图片时，图片必须放在 workspace 目录下，然后用 `openclaw message send` CLI 发送

## ClawUI 持续改进 (cron 自动)
- 00:58: Snap Chromium profile 路径修复 (commit 3b28f9a)
- 02:03: snap launcher 检测改进 (commit aa72b3b)
- 03:11: 仓库整理 + pyproject.toml 打包 (commit f58503d)
- 04:17: CLI 入口 clawui run/apps/tree/screenshot/test/version (commit eed9286)
- 05:21: wait_for_element/wait_for_text 工具实现 (commit c67f305)
- 06:27: click_element/get_element_text 高层工具 (commit a3c02f2)
- 07:31: CDP 持久化 WebSocket 连接, 15-30x 加速 (commit 6a6d88c)

## 微信开发者工具 Wine 问题深度修复 (10:12-11:03)
- **模拟器黑屏 + 按钮无响应**:
  - 项目创建成功（飞机大战模板 minigame-1），但 IDE 模拟器黑屏、编译/预览按钮无反应
  - 根因1: Wine 默认模拟 Win7，微信开发者工具 1.06+ 不再支持 Win7
  - 根因2: GPU/Canvas 渲染在 Wine 下需要 swiftshader 后端
  - 修复: `winecfg /v win10` 切换到 Win10 + 清理 nwjs GPU/Shader/Code Cache
  - 修复后 UI 交互恢复正常（创建项目弹窗可正常操作）
- **小游戏项目已创建**:
  - 路径: `~/.wine/drive_c/users/hung/WeChatProjects/minigame-1/`
  - 模板: 飞机大战（hero/enemy/bullet/explosion/bgm）
  - 需继续: 编译 + 模拟器预览 + 真机测试
- **启动脚本已更新**: `~/launch_wechat_devtools.sh`（含 Win10 设置 + 缓存清理 + swiftshader）
- **ClawUI 自动化微信开发者工具的关键经验**:
  - Wine 应用不在 AT-SPI 树中，必须用 xdotool 坐标 + 截图驱动
  - 菜单栏点击需精确像素定位（OCR 在 Wine 窗口中不可靠）
  - 绿色按钮可通过 PIL/numpy 颜色匹配找到坐标

## ClawUI CI/Test Suite (cron 11:55)
- **Implemented**: Unit test suite (14 tests) + GitHub Actions CI + fixed e2e tests
  - `tests/test_unit.py`: 14 unit tests covering screenshot, CDP, actions, X11, agent tools, recorder, OCR, perception, CLI — all run without display server
  - `tests/test_e2e.py`: rewritten with proper pytest assertions and skip decorators (no more return-bool warnings)
  - `tests/conftest.py`: shared path setup
  - `.github/workflows/ci.yml`: runs unit tests on Python 3.10/3.11/3.12 + syntax lint
- **All tests pass**: 14 unit + 4 e2e = 18 tests green
- **Commit**: `3fdfcec` pushed to main
- **GitHub Issues**: gh CLI not authenticated; could not check (known issue from 2026-03-13)

## ClawUI: Annotated Screenshots Feature (cron 13:03)
- **Implemented**: `annotated_screenshot` + `click_by_index` tools (commit a117629)
  - `src/annotated_screenshot.py`: Takes screenshot, detects interactive elements via AT-SPI tree walk or CDP DOM query, overlays numbered red labels
  - `click_by_index`: Click any labeled element by number — no more coordinate guessing
  - Agent loop updated to handle `image_and_text` tool result type
  - Tested: 20 elements detected on current desktop (buttons, menus, toggles)
- **Test suite fixed**: Renamed `test()` helper → `run_test()` to avoid pytest fixture conflict; removed `return True` warnings
  - 23/23 tests pass, 0 errors, 0 warnings (down from 1 error + 25 warnings)
- **GitHub Issues**: gh CLI not authenticated; cannot check (known issue)
- **Impact**: This is the #1 reliability improvement for AI-driven GUI automation — numbered element labels eliminate coordinate guessing errors
- **Total tools**: 58 (up from 56)

## Heartbeat Checkpoint (13:30)
- TODO.md 检查完成：当前条目均已完成，无需额外安全执行任务。
- 微信基础库下载进展：`3.14.2.wxvpkg` 已通过直连下载成功（约 34MB），替换了此前 0-byte 损坏文件。
- NVIDIA API 测试：`https://integrate.api.nvidia.com/v1` + `z-ai/glm4.7` 连通，返回结果主要在 `reasoning_content` 字段。
- 工作区 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`, `s2.png`, `s3.png`, `s4.png`, `s5.png`, `s6.png`

## ClawUI Continuous Improvement (cron 14:12)
- **Reviewed**: TODO.md (all items complete), GitHub issues (0 open)
- **Found**: Remote already had annotated_screenshot from earlier cron (commit a117629), but our local was behind
- **Action**: Rebased local changes, added 3 unit tests for annotated_screenshot module (import, dedup, LabeledElement serialization)
- **Tests**: 17/17 unit tests pass
- **Commit**: `41bd129` pushed to main
- **Tool count**: 60 agent tools
- **Status**: Project is mature — all core features implemented, CI green, no user issues

## Heartbeat Checkpoint (13:58)
- TODO.md 检查完成：无新增可安全自动执行任务。
- 记忆维护检查：MEMORY.md 已包含今日关键结论（微信基础库 IPv4 下载可绕过、NVIDIA Integrate 返回 `reasoning_content` 兼容性要点），暂无额外需补充项。
- 工作区 git 状态（仍有未提交变更）：
  - Modified: `ClawUI`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`, `s2.png`, `s3.png`, `s4.png`, `s5.png`, `s6.png`

## Heartbeat Checkpoint (14:53)
- 已按 HEARTBEAT.md 执行检查：TODO.md 当前无可安全自动执行的新任务。
- 近期记忆与 MEMORY.md 已复核，暂无需要新增的长期记忆条目。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`, `s2.png`, `s3.png`, `s4.png`, `s5.png`, `s6.png`, `scripts/run_wechat_import_recover.sh`

## Heartbeat Checkpoint (14:55)
- 已检查 TODO.md：无新增可安全自动执行任务。
- 已复核最近记忆并将“微信开发者工具导入页绕过”关键结论写入 MEMORY.md（CLI `open --project`、Service Port 交互点、WeappLocalData 索引注入兜底）。
- 当前 git 状态（仍有未提交变更）：
  - Modified: `ClawUI`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`, `s2.png`, `s3.png`, `s4.png`, `s5.png`, `s6.png`, `scripts/run_wechat_import_recover.sh`

## 飞书图片发送流程复盘 (15:18)
- 问题：先前用 `openclaw message send --media <path>` 直接发送失败，报错 `required option '-t, --target <dest>' not specified`。
- 原因：该命令在当前环境需要显式 target；而本会话内最稳妥的“给当前用户发图”方式是直接用 `read <image_path>` 让渠道原生附图回传。
- 结论（以后默认执行）：
  1. 当前会话回图：优先 `read` 图片文件（workspace 或 /tmp）
  2. 跨会话/指定目标发图：再用 `openclaw message send --target ... --media ...`
- 备注：已向 KK 重新发送二维码图（通过 `read /tmp/wechat_step3.png`）并成功可见。

## Heartbeat Checkpoint (15:28)
- 已检查 TODO.md：无新增可安全自动执行任务（现有条目均完成）。
- 已复核近期记忆：当前关键事项仍是微信开发者工具登录二维码刷新与后续导入验证，无新增需写入 MEMORY.md 的长期结论。
- git 状态：存在未提交变更。
  - Modified: `ClawUI`, `MEMORY.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`~`s6.png`, `scripts/run_wechat_import_recover.sh`, `wechat_qr.png`, `wechat_qr_fresh.png`

## Heartbeat Checkpoint (15:30)
- 已检查 TODO.md：无新增可安全自动执行任务。
- 已复核近期记忆并更新 MEMORY.md：修正飞书发图流程（当前会话优先 `read` 附图；跨会话/指定目标才用 `openclaw message send --target ... --media ...`）。
- git 状态：存在未提交变更。
  - Modified: `ClawUI`, `MEMORY.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`, `s2.png`, `s3.png`, `s4.png`, `s5.png`, `s6.png`, `scripts/run_wechat_import_recover.sh`, `wechat_qr.png`, `wechat_qr_fresh.png`

## Heartbeat Checkpoint (15:33)
- 已检查 TODO.md：无新增可安全自动执行任务（全部已完成）。
- 复核近期记忆与 MEMORY.md：已包含关键结论（微信开发者工具 Wine 自动化、飞书发图路径限制与发送策略、CLI 绕过导入页）。
- 当前 git 状态（存在未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`~`s6.png`, `scripts/run_wechat_import_recover.sh`, `wechat_qr*.png`

## Heartbeat Checkpoint (15:41)
- 已检查 TODO.md：无新增可安全自动执行任务。
- 已复核 MEMORY.md：新增经验已覆盖“飞书发图路径限制 / 微信导入绕过 / NVIDIA 返回字段”等关键结论，暂无需追加长期记忆。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `cli2.png`, `cli_open.png`, `s1.png`, `s2.png`, `s3.png`, `s4.png`, `s5.png`, `s6.png`, `scripts/run_wechat_import_recover.sh`, `wechat_qr*.png`

## Heartbeat Checkpoint (15:43)
- 已检查 TODO.md：当前无可安全自动执行的新任务。
- 当前 git 状态仍有未提交变更：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `scripts/run_wechat_import_recover.sh`, 多个二维码/调试截图（`wechat_qr*.png`, `s1~s6`, `cli*.png`）

## Heartbeat Checkpoint (15:55)
- TODO.md 已检查：无新增可安全自动执行任务（全部为已完成项）。
- MEMORY 复核：当前关键问题集中在 Wine + 微信开发者工具导入流程与二维码/登录态波动，暂无需新增长期原则条目。
- git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`, `skills/gui-automation/demos/github_repo_creation.py`, `skills/gui-automation/src/cdp_helper.py`
  - Untracked: `scripts/run_wechat_import_recover.sh`, `wechat_qr*.png`, `cli*.png`, `s*.png` 等调试截图/脚本文件

## 微信开发者工具自动化继续 (15:13-16:53)
- **二维码发送问题**: 飞书发图必须从 workspace 目录发（/tmp 路径被 OpenClaw 拒绝 `path-not-allowed`），需先 `cp` 到 workspace 再用 `openclaw message send --media`。
- **二维码过期循环**: 多次刷新+重发二维码给 KK，但因截图时机/工具会话状态问题，二维码频繁过期。最终发现工具已自动登录（进入"创建小程序"页面，不再需要扫码）。
- **CLI open 尝试**: 用 `cli.bat open --project` 命令尝试打开项目，触发 `DirectoryNotFoundException: WeappLocalData/localstorage_*.json.lock`，手动 `mkdir -p` 补齐缺失目录后修复。
- **xdotool 输入不生效**: 在"创建小程序"对话框中，xdotool type 未能成功修改项目名和目录字段（字段仍显示旧值 `miniprogram-2`），需要更精准的输入方式（双击选中+清除后再输入）。
- **当前卡点**: 停在"创建小程序"页，需解决 xdotool 对 Wine NW.js 输入框的兼容问题。

## ClawUI Continuous Improvement (cron 17:27)
- **Fixed critical bug**: `cdp_wait_for_selector` and `cdp_wait_for_navigation` referenced undefined `inp` variable instead of `input_data` — would crash at runtime when called
- **Added `_with_retry` decorator**: Reusable retry helper to DRY up the ~400 lines of duplicated retry logic across agent tools (available for future refactoring)
- **All 23 tests pass**
- **GitHub Issues**: 0 open issues
- **Commit**: `f7a3de0` pushed to main

## ClawUI Continuous Improvement (cron 17:27)
- **Implemented**: CLI v0.2.0 — expanded from 5 to 13 commands (commit be52f5b)
  - New: `elements`, `find`, `click`, `type`, `key`, `browser`, `record`, `replay`
  - Fixed broken `test` command (now uses pytest properly)
  - All commands use lazy imports + graceful ImportError handling
  - Version bumped to 0.2.0 in cli.py + pyproject.toml
- **Tests**: 37/37 pass (up from 23)
- **GitHub Issues**: 0 open issues on longgo1001/clawui
- **Impact**: Users can now automate GUI tasks from CLI without needing the AI agent loop

## Heartbeat Checkpoint (17:17)
- 已检查 TODO.md：无新增可安全自动执行任务。
- 已复核并更新 MEMORY.md：补充两条关键经验
  1) 微信开发者工具增加 `--remote-debugging-port=9222` 后可用 CDP 直接操控 DOM，绕过 xdotool 在 Wine 下输入/点击不稳定。
  2) “创建”按钮不进入 IDE 的关键原因之一是 `AppID` 为空（界面红字“无 AppID”），需有效 AppID 或可用测试号路径。
- git 状态：存在未提交变更（当前看到 `ClawUI` 有改动标记）。

## Heartbeat Checkpoint (17:40)
- TODO.md 已检查：无新增可安全自动执行任务（当前清单均为已完成项）。
- MEMORY.md 复核：暂无新增需固化的长期原则（当前问题仍是 Wine + 微信开发者工具渲染/交互不稳定）。
- git 状态（有未提交变更）：
  - Modified: `MEMORY.md`, `memory/2026-03-14.md`
  - Changed (submodule/content): `ClawUI`

## Heartbeat Checkpoint (17:55)
- 已按 HEARTBEAT.md 执行：TODO.md 检查完成，当前无可安全自动执行的新任务。
- 记忆维护：已将 ClawUI CLI v0.2.0（新增 elements/find/click/type/key/browser/record/replay + test 修复）写入 MEMORY.md 作为长期变更记录。
- git 状态（有未提交变更）：
  - Changed (submodule/content): `ClawUI`
  - Modified: `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_screen_now.png`

## Heartbeat Checkpoint (19:00)
- 已检查 TODO.md：无新增可自动执行任务（当前任务列表均为已完成项）。
- 已复核并更新 MEMORY.md：补充了 3 条关键长期结论（双窗口ID误命中、Wine输入事件兼容性、原生Linux版迁移与CLI service port限制）。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (19:12)
- TODO.md 已检查：无新增可安全自动执行任务（均已完成项）。
- 已复核近期记忆并更新 MEMORY.md：补充了“同名窗口误命中导致点击无效”“Wine 下 click 与 mousedown/mouseup 行为差异”“已迁移原生 Linux 版微信开发者工具”的长期结论。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (19:23)
- TODO.md 已检查：无新增可安全自动执行任务。
- 根因补充：微信开发者工具在自动化中出现“同名双窗口 ID”导致事件打到错误窗口；同时 Wine/NW.js 下 `xdotool click` 兼容性差，`mousedown/mouseup` 更稳定。
- 原生迁移进展：已安装 `msojocs/wechat-web-devtools-linux`（v2.01.2510280-2），原生版已可登录并打开“创建小程序”页。
- CLI 阻塞提示：工具服务端口关闭（需在工具安全设置开启）。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (19:45)
- TODO.md 已检查：无新增可安全自动执行任务（均为已完成项）。
- 当前定位到新关键结论：
  1) 原生 Linux 版微信开发者工具可启动并登录（替代 Wine 可行）。
  2) “创建”失败的直接原因是 `AppID` 未填（界面提示“无 AppID”）。
  3) 交互层面存在稳定性差异：`xdotool click` 在部分控件不稳定，`mousedown+mouseup` 更可靠；并且历史上存在同名多窗口 ID 导致点错窗口的问题。
- git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (19:58)
- 已检查 TODO.md：当前无新增可安全自动执行任务。
- 已复核近期记忆并更新 MEMORY.md：补充 `agent.py` 中 CDP/Marionette 23 处重试逻辑用 `_with_retry` 收敛，以及当前测试入口以 `pytest -q` 为准（该分支无 `tests/test_unit.py`）。
- git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (20:05)
- 已按 HEARTBEAT.md 执行检查：TODO.md 无新增可安全自动执行任务。
- 近期记忆复核：暂无新的长期记忆条目需要写入 MEMORY.md。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`
- 备注：检测到一次 `git push` 网络中断（Broken pipe / remote hang up），后续推送需重试。

## 微信开发者工具创建失败排查方法（20:00+）
- 采用“截图→OCR识别→分支修复”流程定位问题，而非盲点按钮。
- OCR 明确识别到关键阻塞提示：`无 AppID`，这就是“点击创建无反应”的根因（前端校验拦截）。
- 修复策略：
  1) 优先处理校验提示（设置测试号/填写有效 AppID）。
  2) 再执行创建动作，观察界面是否进入下一阶段（创建小程序 → 创建小游戏）。
  3) 若按钮仍无响应，继续截图+OCR读取最新提示，再分支处理。
- 实战结果：填入有效格式 AppID 后，界面从“创建小程序”推进到“创建小游戏”，证明错误提示驱动的分支流程有效。

## ClawUI 流程优化（已落地）
- 在 `skills/gui-automation/src/ocr_tool.py` 新增 `ocr_extract_lines()`：可提取全屏 OCR 文本行（含位置/置信度）。
- 在 `skills/gui-automation/src/agent.py` 新增 `screen_inspect` 工具：
  - 自动检测错误/提示关键词（如 `无 AppID`、`重新登录`、`错误`、`失败`、`网络`）。
  - 输出命中提示与建议动作，支持“先理解画面，再决定后续步骤”。

## 原生版推进成功 + 新错误定位（20:13）
- 通过 OCR 定位“创建”按钮中心点并触发点击，成功从“创建小游戏”进入 IDE 主界面。
- 新阻塞错误已识别：
  - 弹窗：`下载基础库版本 3.14.2 失败: read ECONNRESET`
  - 控制台：`Failed to load resource ... status 500 (Internal Server Error)`
- 结论：创建流程已打通，当前卡在网络/资源下载层（非按钮/输入问题）。
- 下一步修复方向：点击“重试”+ 网络/代理可达性检查（对 `res.servicewechat.com`）。

## Heartbeat Checkpoint (20:15)
- TODO.md 已检查：无新增可安全自动执行任务（当前条目均已完成）。
- MEMORY.md 复核：晚间新增的关键经验（OCR先识别错误再操作、AppID 校验、ECONNRESET 网络根因）已覆盖，暂无需重复追加。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (20:30)
- 已按 HEARTBEAT.md 执行：TODO.md 检查完成，当前无可安全自动执行的新任务。
- 已复核近期记忆并更新 MEMORY.md：补充“截图→OCR→分支修复”的故障分流方法，以及 `ocr_extract_lines`/`screen_inspect` 的长期价值；当前主要瓶颈已转为基础库下载网络层错误（ECONNRESET/500）。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`, `skills/gui-automation/src/agent.py`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (20:45)
- TODO.md 已检查：无新增可安全自动执行任务（均已完成）。
- 记忆维护：今日重要经验已补充（OCR 驱动错误分支 + AppID/ECONNRESET 排查流程），暂不额外修改 MEMORY.md。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `scripts/e2e_wechat_minigame.py`, `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (21:09)
- TODO.md 已检查：无新增可安全自动执行任务（当前条目均已完成）。
- 已复核近期记忆与 MEMORY.md：当前长期结论已覆盖核心经验（OCR 分流、AppID 门槛、网络层 ECONNRESET/500），本轮无需新增长期记忆。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (21:10)
- 已按 HEARTBEAT.md 执行：TODO.md 复核完成，当前无可安全自动执行的新任务。
- MEMORY.md 复核：当前长期记忆已覆盖今日关键结论，无新增需要提升的条目。
- 当前 git 状态（有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## Heartbeat Checkpoint (21:11)
- TODO.md 已检查：无新增可安全自动执行任务。
- 已更新 MEMORY.md：补充“截图+OCR先识别后操作”“无AppID/ECONNRESET分流处理”“原生Linux版优先”等长期经验。
- 当前 git 状态（仍有未提交变更）：
  - Modified: `ClawUI`, `MEMORY.md`, `memory/2026-03-14.md`
  - Untracked: `wechat_native_qr.png`, `wechat_screen_now.png`

## ClawUI 流程再升级（21:16+）
- 新增 `smart_step` 工具：实现 Observe→Decide→Act→Verify 单步闭环。
- 行为逻辑：
  - 先 OCR 理解页面全文（before_text）
  - 根据阻塞提示分类决策（`ECONNRESET`→重试，`无AppID`→测试号，创建页→创建）
  - 执行动作后再次截图 OCR（after_text）
  - 输出 `changed/still_blocked` 验证结果，避免盲点循环
- 目标：把“先理解再操作”固化到可复用工具，减少人工提示依赖。
